A Rails app for Restaurants to build a simple page and customers to reserve a table.

Notes:
Uses Mailcatcher in development environment


Note:
```
$("tr").each(function(i,e){console.log($(e).data("restaurant"));});
```