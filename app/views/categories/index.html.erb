<h3>Find Reservster Categories Fast</h3>



<% @categories.each do |category| %>
	<p>
	<%= link_to(category.name, {:action => 'show', :id => category.id} ) %>
		<span class="badge badge-info"><%= category.restaurants.count %></span>
	</p>
<% end %>
